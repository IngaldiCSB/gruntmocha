/**
 *  Unit tests root file
 *  
 *  TODO: in complex app design, would be nice to create a 'test' folder 
 *  that reflects the development source tree, so for each module 'app/subdir/mymodule'
 *  there would be a 'test/subdir/mymodule_tests'. This keeps clean project organization
 *   
 */


var chai = require ('chai');



describe('Array', function(){
  describe('#indexOf()', function(){
    it('should always fail', function(){
         chai.assert.equal(1 , 2);
    });
  });
});
